# ============================================================================
# MANT 核心依赖（与 pyproject.toml 保持一致；推荐 pip install -e . 安装）
# ============================================================================
langgraph>=0.2        # LangGraph 状态机（QA 回退返工回环）
openai>=1.0           # LLMClient 统一调用大模型（fast/strong 双档）
pyyaml>=6.0           # 读取 config/settings.yaml
numpy>=1.26           # 向量与数值计算
python-dotenv>=1.0    # 从 .env 加载 API key 等环境变量
chardet>=5.2          # TXT 编码识别并统一转换为 UTF-8

# ----------------------------------------------------------------------------
# 可选依赖（按需手动安装）：
#   faiss-cpu>=1.7.4    # FAISS 向量检索（RAG）
#   pytest>=8.0         # 开发 / 测试
# M1 句对齐工具 vecalign / LASER 通常需从源码安装：
#   pip install git+https://github.com/thompsonb/vecalign.git
#   https://github.com/facebookresearch/LASER
# ----------------------------------------------------------------------------
